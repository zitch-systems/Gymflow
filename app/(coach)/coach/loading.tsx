import { ConsoleSkeleton } from '@/components/skeletons';

// Instant sidebar-navigation feedback — the shell stays put while the page
// area shows this skeleton until the dynamic payload lands. Nested segments
// carry their own copies (a boundary only fires when its child changes).
export default ConsoleSkeleton;
